name: mimecast_get_threat_incidents returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: mimecast_get_threat_incidents
  args: {}
  expect:
    is_error: true
timeout: 30s
