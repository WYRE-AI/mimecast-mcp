{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.WYRE-AI/mimecast-mcp",
  "title": "Mimecast",
  "description": "MCP server for Mimecast email security: message queue, held messages, domains, and threats.",
  "repository": {
    "url": "https://github.com/WYRE-AI/mimecast-mcp",
    "source": "github"
  },
  "version": "0.0.0",
  "websiteUrl": "https://github.com/WYRE-AI/mimecast-mcp",
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/wyre-ai/mimecast-mcp:0.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MIMECAST_CLIENT_ID",
          "description": "Mimecast API 2.0 client ID",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "MIMECAST_CLIENT_SECRET",
          "description": "Mimecast API 2.0 client secret",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MIMECAST_REGION",
          "description": "Mimecast region: 'us' (default), 'eu', 'de', 'au', 'za', 'ca', 'usb', 'usbe', or 'gb'",
          "isRequired": false,
          "default": "us",
          "format": "string"
        },
        {
          "name": "MCP_TRANSPORT",
          "description": "Transport mode for the server. Set to 'stdio' for local CLI use; the image defaults to 'http' for gateway hosting.",
          "isRequired": false,
          "default": "stdio",
          "format": "string"
        },
        {
          "name": "AUTH_MODE",
          "description": "Credential source: 'env' reads vars locally, 'gateway' expects header injection from the WYRE MCP Gateway.",
          "isRequired": false,
          "default": "env",
          "format": "string"
        },
        {
          "name": "LOG_LEVEL",
          "description": "Log verbosity: debug, info, warn, error",
          "isRequired": false,
          "default": "info",
          "format": "string"
        }
      ]
    }
  ]
}
